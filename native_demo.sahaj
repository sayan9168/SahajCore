fn add(a, b) {
    return a + b
}

fn factorial(n) {
    if n <= 1 {
        return 1
    }
    return n * factorial(n - 1)
}

fn main() {
    let x = 10
    let y = 20
    let sum = add(x, y)
    print(sum)
    print(factorial(5))
}
